const initState = {
    datas: [],
    data: {},
    loading: false,
};

export const dataReducer = (state = initState, action) => {
    switch (action.type) {
        default: 
            return state;
    }
}